#ifndef TURBO_PI_DOG_TRACKER_STREAM_INFERENCE_HPP
#define TURBO_PI_DOG_TRACKER_STREAM_INFERENCE_HPP
#pragma once


#include "cli.hpp"

int run_live(const Configuration& configuration);
int run_render(const Configuration& configuration);

#endif // TURBO_PI_DOG_TRACKER_STREAM_INFERENCE_HPP
